paths-ignore:
  # This reviewed corpus contains generated credentials that intentionally match
  # provider formats. It validates Hide Secrets and never contains live secrets.
  - "Tests/Fixtures/Secrets/**"
