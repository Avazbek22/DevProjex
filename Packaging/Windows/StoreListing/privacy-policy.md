# Privacy Policy — DevProjex

Last updated: 2026-09-03

DevProjex is a desktop application that helps users visualize folder/project structures, optionally read the contents of user-selected text files, and copy or export generated results. The app is designed to be read-only with respect to the source projects you open.

## 1) Data We Collect

DevProjex does not collect, store, or transmit personal data.

DevProjex does not require an account and does not use analytics, tracking, advertising identifiers, or telemetry.

## 2) Files and Folder Access

DevProjex accesses files and folders only when you explicitly select a folder/repository to open or clone.

The app reads directory names and file names to build a tree view.

If you choose a "copy content" action, the app reads the contents of the selected text files in order to place that text into your clipboard.

DevProjex does not modify, delete, or upload files in the source projects you open. When you explicitly export a context document, folder copy, ZIP archive, or profile, the app writes that generated result to the destination you choose.

## 3) Clipboard

When you use copy features, DevProjex places the generated tree and/or selected text content into your system clipboard. Clipboard contents are controlled by your operating system and other apps may be able to read clipboard data according to OS rules.

## 4) Network and Git Features

DevProjex may access the network when you use Git-related features (for example, cloning a repository, fetching repository updates, or listing branches) or explicitly check for an application update. If you opt in to weekly update checks, the app may also request the latest stable DevProjex release metadata from GitHub at startup after at least seven days have elapsed since the previous check.

Application update checks send no project files or application settings. They read only the public latest-release version from the DevProjex GitHub repository; the app never downloads or installs an update automatically.

DevProjex does not send your local files to any server and does not upload changes to remote repositories (no commit/push/merge).

## 5) MCP Server and AI Clients

DevProjex includes an optional Model Context Protocol (MCP) server that AI clients running on your device (for example, coding agents) can connect to. The server exposes read-only tools, is confined to the folders you allow, and always masks detected secrets in tool output. Remote repository access over the network stays disabled unless you explicitly opt in.

DevProjex itself does not transmit the content these tools return anywhere. If the AI client you connect sends that content to an online service, that transfer happens outside DevProjex and is governed by the client's own privacy policy.

## 6) Local Storage

DevProjex stores application settings locally on your device (such as UI preferences, language, themes/effects, and ignore rules).

If you use Git features, repository data may be cached locally between sessions to avoid unnecessary downloads. You can inspect or remove this managed cache through DevProjex.

## 7) Third-Party Services

DevProjex does not integrate with third-party analytics, advertising, or tracking services.

## 8) Children's Privacy

DevProjex is not directed to children and does not knowingly collect personal data from children.

## 9) Changes to This Policy

If DevProjex changes in a way that affects privacy, this policy will be updated. The "Last updated" date will reflect the latest revision.

## 10) Contact

If you have questions about this Privacy Policy, contact:

Email: avazbekolimov722@gmail.com
