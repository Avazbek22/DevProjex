DevProjex is a packaged desktop (Win32/Avalonia) application and requires full-trust execution to run the desktop process.
The app never modifies source projects; it writes only user-requested exports and application-owned local state, and it does not perform self-updates.
