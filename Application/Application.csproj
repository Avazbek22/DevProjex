<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>DevProjex.Application</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Kernel\Kernel.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The native compression adapter needs cancellable plan internals without widening the public API. -->
    <InternalsVisibleTo Include="Infrastructure" />
    <InternalsVisibleTo Include="DevProjex.Tests.Unit" />
  </ItemGroup>
</Project>
