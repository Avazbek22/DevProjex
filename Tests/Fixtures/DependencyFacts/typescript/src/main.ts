import { value } from './x.js';
import { missing } from './dir';
import blocked from 'fixture/blocked';
import { Exact } from '@exact';
import { Lib } from '@lib/item';

export interface UsesTypes { exact: Exact; lib: Lib; }
export const answer = value;
