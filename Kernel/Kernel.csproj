<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>DevProjex.Kernel</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Test hosts that arm RepositoryTransportPolicy for a synthetic local remote. No shipped
         assembly is listed here, so product code cannot widen the accepted transport set. -->
    <InternalsVisibleTo Include="DevProjex.Tests.Unit" />
    <InternalsVisibleTo Include="DevProjex.Tests.Integration" />
    <InternalsVisibleTo Include="DevProjex.Tests.Terminal" />
    <InternalsVisibleTo Include="DevProjex.Tests.Terminal.ProgressHost" />
  </ItemGroup>
</Project>
